const config = {
	// Keep trailing commas in multiline structures (objects, arrays, functions)
	trailingComma: "all",

	// Use tabs instead of spaces
	useTabs: true,

	// Common sensible defaults
	semi: true,
	singleQuote: false,
	quoteProps: "as-needed",
	bracketSpacing: true,
	arrowParens: "always",
	endOfLine: "lf",
	printWidth: 100,
};

export default config;
